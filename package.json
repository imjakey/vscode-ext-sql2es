{
  "name": "sql2es",
  "displayName": "%displayName%",
  "description": "%description%",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/imjakey/vscode-ext-sql2es"
  },
  "categories": [
    "Programming Languages"
  ],
  "keywords": [
		"SQL",
		"MySQL",
		"Elasticsearch",
		"ES",
    "Es",
    "DSL",
    "converter",
		"mysql",
		"database",
		"数据库"
	],
  "icon": "images/icon.png",
  "activationEvents": [
    "onCommand:sql2es.convert",
    "onCommand:sql2es.convertSelected",
    "onCommand:sql2es.convertToCurl"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sql2es.convert",
        "title": "%command.convert.title%"
      },
      {
        "command": "sql2es.convertSelected",
        "title": "%command.convertSelected.title%"
      },
      {
        "command": "sql2es.convertToCurl",
        "title": "%command.convertToCurl.title%"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "sql2es.convertSelected",
          "when": "editorHasSelection",
          "group": "navigation"
        },
        {
          "command": "sql2es.convertToCurl",
          "when": "editorHasSelection",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "%config.title%",
      "properties": {
        "sql2es.apiKey": {
          "type": "string",
          "default": "",
          "description": "%config.apiKey.description%"
        },
        "sql2es.apiEndpoint": {
          "type": "string",
          "default": "https://api.openai.com/v1/chat/completions",
          "description": "%config.apiEndpoint.description%"
        },
        "sql2es.model": {
          "type": "string",
          "default": "gpt-3.5-turbo",
          "description": "%config.model.description%"
        },
        "sql2es.esVersion": {
          "type": "string",
          "default": "7.x",
          "description": "%config.esVersion.description%"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "22.17.1",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.28.0",
    "typescript": "^4.9.4"
  }
}
